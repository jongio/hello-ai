{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker Compose Up",
            "type": "node-terminal",
            "request": "launch",
            "command": "docker-compose up --build",
            "cwd": "${workspaceFolder}",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}